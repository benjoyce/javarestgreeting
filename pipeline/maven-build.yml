platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3.5-jdk-8-alpine

inputs:
  - name: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "clean", "install" ]
